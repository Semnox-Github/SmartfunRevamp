import 'package:flutter/foundation.dart';

class Environment {
  // static const baseUrl = (kDebugMode) ? "https://smartfunapp.parafait.com/api/" : "";
  static const baseUrl = (kDebugMode) ? "https://smartfungigademo.parafait.com/api/" : "";
}
